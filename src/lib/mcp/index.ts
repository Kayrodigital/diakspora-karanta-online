import { defineMcp } from "@lovable.dev/mcp-js";

export default defineMcp({
  name: "diakspora-karanta-mcp",
  title: "Diakspora Karanta MCP",
  version: "0.2.0",
  instructions:
    "No learning-data tools are exposed until authenticated, organization-scoped data access is available.",
  tools: [],
});
